{
  "name": "trusty",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "heroku-postbuild": "cd ./trusty-client && yarn install && yarn build && cd ../trusty-backend && yarn install && yarn build",
    "start": "node dist/main"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": "16.x"
  }
}
